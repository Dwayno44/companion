// ── Settings you control ───────────────────────────────────────────────
//
// After you deploy the Cloudflare Worker (see README.md), paste its address
// between the quotes below, replacing the placeholder.
//
// Example:  const WORKER_URL = "https://companion.dwayne.workers.dev";
//
// Until you do, the page runs in a gentle "demo" mode so you can see how it
// looks and feels — the demo replies are canned, not real.

const WORKER_URL = "PASTE_YOUR_WORKER_URL_HERE";
